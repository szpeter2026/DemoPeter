# DemoPeter 知识运营闭环规划

> **版本基线：** `main` @ `84483c1`  
> **目标读者：** 项目团队（产品、后端、全栈、Agent/集成）  
> **最后更新：** 2026-05-24

---

## 1. 背景与现状

DemoPeter（szpeter2026 知识库平台）已完成第一阶段 RAG 能力：

| 能力 | 状态 | 说明 |
|------|------|------|
| 检索 + AI 问答 | ✅ 已可用 | 混合检索 RRF、DeepSeek/Ollama |
| 跨文档缺口推理 | ✅ 刚起步 | RAG 注入文档目录，LLM 推断年份缺口 |
| 缺口可度量、可追踪 | ❌ 未做 | 无覆盖矩阵、无任务池 |
| 内外部自动补料 | ❌ 未做 | Gitea/Outlook 未入库 |
| 人机审核充实闭环 | ❌ 未做 | 无审核队列 |

**核心问题：** 系统会「问、会答、能猜缺口」，但知识库 **不会自己长大**，缺口无法被团队持续运营。

**目标：** 形成可运营闭环：

```
盘点覆盖 → 发现缺口 → 分派/拉取 → 人工审核 → 增量入库 → 复检关闭
         ↑___________________________________________|
```

---

## 2. 价值主张

| 角色 | 价值 |
|------|------|
| 业务/研究 | 明确知道「缺什么」，不依赖每次向 AI 提问 |
| 运维/数据 | 统一入库、增量同步、操作可审计 |
| Agent/MCP | 自动查覆盖、建任务、触发导入 |
| 外部协同 | Gitea、Outlook 等信息 **进入向量库**，而非只写 Notion |

---

## 3. 与现有代码的衔接

以下模块 **可直接复用**，无需推倒重来：

| 模块 | 路径 | 在闭环中的位置 |
|------|------|----------------|
| 统一入库 | `src/kb_service.py`、`scripts/import_corpus.py` | 充实阶段入口 |
| 语料配置 | `config/corpora.json`、`config/corpus_loader.py` | 覆盖规则按 corpus 定义 |
| Agent 入口 | `src/mcp_server.py` | 缺口查询、触发导入 |
| 跨文档推理（临时） | `src/rag_engine.py` → `_doc_catalog_context` | **Phase 1 升级为确定性分析** |
| 查询日志 | `src/db_manager.py` → `query_logs` | Phase 2 失败查询挖掘 |
| 报告中心 | `src/report_gen.py`、`templates/reports.html` | 覆盖度/缺口报表 |
| Gitea 连接器 | `src/gitea_webhook.py` | 今：→ Notion；Phase 3：→ KB |
| Outlook 连接器 | `src/outlook_client.py` | 今：OAuth only；Phase 3：附件入库 |
| Web 导入 | `src/web_dashboard.py` → `/api/documents/import` | **需统一到 kb_service（Phase 1b）** |

---

## 4. 目标架构

```
┌─────────────────────────────────────────────────────────────┐
│                        感知层                                │
│  Coverage Analyzer（覆盖矩阵）  Gap Task Pool（缺口任务池）   │
│  Query Log Miner（失败查询挖掘）                              │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                        协同层                                │
│  Web 报告中心    MCP Tools    Gitea Webhook    Outlook Ingest │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                        执行层                                │
│  KnowledgeBaseService    Incremental Scanner    Review Queue  │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│  SQLite（documents / chunks / gap_tasks）  +  Chroma/pgvector │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 分阶段路线图

### Phase 1 — 可度量的查漏补缺（优先，约 2–3 周）

#### 1a. 结构化覆盖分析（选项 A）

| 项 | 内容 |
|----|------|
| **新建** | `src/coverage_analyzer.py` |
| **规则配置** | 在 `config/corpora.json` 扩展 `coverage_expectations`（按 corpus 定义期望覆盖） |
| **科锐国际示例** | `年份 × {年报, 一季报, 半年报, 三季报}` |
| **API** | `GET /api/coverage/{corpus_id}` |
| **MCP** | `kb_coverage_report(corpus_id?)` |
| **UI** | 报告中心新增「覆盖度」Tab：红/黄/绿矩阵 + 缺口列表 |

**验收标准：**

- 科锐国际：2017 仅 Q3、2020 仅 Q1、2021+ 空白 — 与人工核对一致
- **不依赖 LLM**，结果为确定性规则输出

#### 1b. 统一 import + 增量扫描

| 项 | 内容 |
|----|------|
| **重构** | Web `/api/documents/import` → 调用 `KnowledgeBaseService` |
| **新建** | `scripts/scan_corpus.py` 或 `manage.sh scan` |
| **逻辑** | mtime/hash 对比 `documents.file_path`，仅导入变更文件 |

**验收标准：**

- CLI（`import_corpus`）、Web、MCP（`kb_import`）三条路径行为一致
- corpus 标签、skip/force 语义统一

#### 1c. 缺口任务池（选项 B）

| 项 | 内容 |
|----|------|
| **Schema** | 新增表 `gap_tasks` |
| **字段** | `id, corpus_id, title, gap_type, status, source, created_at, closed_at, metadata JSON` |
| **任务来源** | 覆盖分析自动创建；RAG 0 命中 / `_below_threshold` 可写入 |
| **API** | `GET /POST /PATCH /api/gaps` |
| **MCP** | `kb_gap_list`、`kb_gap_close` |
| **UI** | 报告中心「待补清单」：待处理 / 进行中 / 已关闭 |

**验收标准：**

- 科锐国际自动生成 ≥3 条缺口任务（如 2017 年报、2020 半年报/年报等）
- 每条任务有唯一 ID 和 status

---

### Phase 2 — 从使用行为驱动补料（约 1–2 周）

- 挖掘 `query_logs`：0 命中、低相似度、含「缺失/补齐」等关键词
- 自动合并到 `gap_tasks`（去重）
- `report_gen.py` 周报增加「本周未答好 Top N」

---

### Phase 3 — 内外部自动补料（选项 C，约 2–4 周）

| 连接器 | 改造点 | 建议优先级 |
|--------|--------|------------|
| **Gitea** | push 变更 `.md/.pdf` → `kb_service.import_directory` | 高 |
| **Outlook** | 指定文件夹附件 → `doc_processor` → import | 中 |
| **Notion** | 选定 DB 导出 MD → import | 低 |

**前置依赖：** Phase 1b 统一 import 必须先完成。

---

### Phase 4 — 人机审核充实闭环（约 2–3 周）

```
gap_task → Agent 拉草稿/外链 → Web「待充实」→ 人工确认
         → 写入语料目录 → scan/import → coverage 复检 → close task
```

---

## 6. 选项 A / B / C 决策建议

| 选项 | 投入 | 依赖 | 建议 |
|------|------|------|------|
| **A 覆盖度矩阵** | 中 | corpora 规则配置 | **必做** — 闭环起点 |
| **B 缺口任务池** | 中 | A 的输出 | **必做** — 让缺口可追踪 |
| **C 自动入库** | 高 | 1b 统一 import | Phase 3，不抢 Phase 1 |

### 推荐 MVP 范围

> **Phase 1 = A + B + 1b（统一 import）**

理由：

1. 科锐国际场景已验证「能推理缺口」，缺的是 **持久化 + 可视化 + 任务化**
2. 不做 1b，Gitea/Outlook 接入会重复 Web/CLI 不一致的问题
3. 没有 A/B，自动入库了也无法确认是否补全

---

## 7. Sprint 拆分建议

| Sprint | 交付物 | 建议负责人 |
|--------|--------|------------|
| **S1** | `coverage_analyzer` + API + 科锐国际规则 + 单元测试 | 后端 |
| **S2** | 报告中心覆盖度页 + `gap_tasks` 表 + 自动建任务 | 全栈 |
| **S3** | 统一 Web import + `manage.sh scan` + MCP 工具扩展 | 后端 / Agent |
| **S4** | 失败查询挖掘 + 周报 | 后端 |
| **S5+** | Gitea/Outlook 入库 + 审核队列 | 集成 / 全栈 |

---

## 8. 成功指标（OKR 对齐）

| 指标 | 当前 | Phase 1 目标 |
|------|------|--------------|
| 缺口识别方式 | LLM 临时推理 | 确定性规则 + 任务 ID |
| 科锐国际覆盖可视化 | 无 | 矩阵页一屏看清 |
| 缺口任务可追溯 | 无 | 每条有 status + 来源 |
| 入库入口 | 3 套逻辑 | 1 套 `kb_service` |
| MCP 工具数 | 4 | ≥6（+coverage, +gaps） |

---

## 9. 团队评审需拍板的三件事

1. **覆盖规则谁维护？**  
   建议：放 `config/corpora.json` 的 `coverage_expectations` 段，业务可配、开发可扩展。

2. **缺口任务谁关闭？**  
   - 方案 A：人工确认后关闭  
   - 方案 B：re-scan 后覆盖达标自动关闭  
   建议：Phase 1 用方案 A，Phase 4 引入方案 B。

3. **Phase 3 先做 Gitea 还是 Outlook？**  
   - 代码协作为主 → Gitea  
   - 邮件财报为主 → Outlook  

---

## 10. 科锐国际语料参考（Phase 1 验收样例）

当前融合语料（`config/corpora.json` → `careerintl`）去重后约 10 份：

| 年份 | 已有 | 缺口 |
|------|------|------|
| 2017 | 第三季度报告 | 一季报、半年报、**年报** |
| 2018 | 一季报、半年报、三季报、**年报** | — |
| 2019 | 一季报、半年报、三季报、**年报** | — |
| 2020 | 第一季度报告 | 半年报、三季报、**年报** |
| 2021+ | — | **全部** |

此表为 Phase 1 覆盖分析器的 **黄金验收用例**。

---

## 11. 相关命令（当前可用）

```bash
./scripts/manage.sh start      # 启动 Web 面板
./scripts/manage.sh import     # 融合语料导入
./scripts/manage.sh mcp        # MCP 服务
./scripts/push-with-token.sh main  # 推送代码（需 macos.token）
```

MCP 现有工具：`kb_stats`、`kb_search`、`kb_ask`、`kb_import`

Phase 1 待增：`kb_coverage_report`、`kb_gap_list`、`kb_gap_close`

---

## 12. 结论

**第一阶段锁定 Phase 1（A + B + 统一 import），约 2–3 周。**

完成后 DemoPeter 从「会答的 RAG」升级为「会管缺口的知识库」；Phase 3 的 Gitea/Outlook 才有明确挂载点。

---

## 附录：变更记录

| 日期 | 说明 |
|------|------|
| 2026-05-24 | 初版：团队实施蓝图，基线 84483c1 |
