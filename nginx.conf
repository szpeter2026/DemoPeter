# szpeter2026 Nginx 反向代理配置
# 阿里云 ECS 上: 将此文件放在项目根目录，docker compose 会自动挂载

upstream flask_app {
    server web:5200;
}

server {
    listen 80;
    server_name _;  # 替换为你的域名，如 kb.yourdomain.com

    # 上传限制（导入文档用）
    client_max_body_size 50m;

    # 静态文件直接由 Nginx 返回
    location /static/ {
        alias /app/static/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # API 和页面请求反向代理到 Flask
    location / {
        proxy_pass http://flask_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 长连接支持（RAG 查询可能需要较长时间）
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }
}

# === 开启 HTTPS 时使用下方配置（需先申请 SSL 证书） ===
# server {
#     listen 443 ssl http2;
#     server_name kb.yourdomain.com;
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     client_max_body_size 50m;
#
#     location /static/ {
#         alias /app/static/;
#         expires 7d;
#     }
#
#     location / {
#         proxy_pass http://flask_app;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 120s;
#     }
# }
#
# server {
#     listen 80;
#     server_name kb.yourdomain.com;
#     return 301 https://$host$request_uri;
# }
